[phases.setup]
nixPkgs = ["nodejs-18_x"]

[phases.install]
cmds = ["npm init -y && npm install serve"]

[phases.build]
cmds = ["echo 'Static site - no build needed'"]

[start]
cmd = "npx serve -s . -l $PORT"